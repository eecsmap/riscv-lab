# M3最终验收（2026-09-16）

结论：M3旧SoC/RV64I启动仿真阶段验收通过。最后的AXI响应因果缺口已闭合，结合历轮独立功能及退出专项验证，允许进入首次上板准备；不表示已经综合、布线或上板。

## 本轮独立核查

- replay2新AXI checker重放run-b的8项正常日志与3项尾部日志，11项均退出0。
- 18项AXI探针全部符合期望：14负向拒绝，4合法时序接受。独立再次把全部R/B移到cycle0，两项均退出1，原漏洞不再复现。
- 源码按cycle分组推进，只让当前已存在的AR承担R、AW与完整W承担B；缓存合法早到W，同ID有序、跨ID可乱序。末尾遗留检查存在，时间线不再预装未来请求。
- 最新regate的8项正常日志独立重放CPU事件checker，按各ELF的exit_store/tohost、标记、IRQ/同步异常计数全部通过；74项源码/ELF/模拟器输入哈希通过。replay2自身源码清单校验通过。
- Claude额外自查的HOSTDONE/SETTLED/AXI汇总重复记录和追加异常，独立4探针均拒绝；增强后的M2-3事件checker重放已验收run-a仍fails=0。

输出目录：codex-review-k8hKiY。本轮是代码审查、归档日志重放和检查器探针，不是再次运行全部硬件仿真；未重编译。此前独立8项SoC运行及退出四模式证据见m3/CODEX_REVIEW.md与m3-fixes/TAIL_CODEX_REVIEW.md，历史问题不删除。

## 验收范围

教学CPU单口/单在途经M1桥、旧TL/AXI与SimAXIMem，专用RV64I ROM、TSI/MSIP/mret启动、HTIF标记和退出、CLINT基本闭环及DDR仿真访问。错误退出可能原始进程返回0，必须连同严格事件checker作为最终结果，不能单看进程退出码。AXI证据属于含TSI的整个端口，不等于CPU专属流量或完整AXI协议形式验证。

检查器面向现有生成日志；对任意损坏日志的字段完整性/范围校验仍可持续增强，不将“通过探针”宣称为完备验证。部分字段有默认值，未来接新日志源应改为强制schema；不改变本次完整日志的结论。

## 下一步

首次上板准备：确认教学板级顶层与现有PS AXI接口兼容，准备独立构建配置、资源/时序验收、回退与测试清单；再明确授权Vivado与板资源操作。M/C仍后置，xv6仍需后续ISA/特权/虚存等工作。当前未测试PLIC外设中断、块设备/DMA并发、真实DDR硬件，未上板。
