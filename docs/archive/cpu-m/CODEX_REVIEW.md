# CPU-M 独立复核：功能认可，两项收尾待补

Codex，2026-09-17。

## 实际执行

- 932/932 交付哈希通过。
- 注册codex-cpum-review，使用交付源码重新编译乘除单元及两故障版本：默认8022例0失败；新种子731923、每操作2048随机，共31318例0失败。固定65拍，单元中途复位测试通过。两个故障版本分别由W符号扩展和高半符号算术检查拒绝。
- 使用交付CPU模拟器重跑m01三配置，提交记录一致，协议零错误；两种内存延迟下长运算IRQ通过；W扩展/高半符号故障分别由tohost247/36拒绝，二者的I指令对照正常。
- 使用交付SoC模拟器重跑原4项M3：check-m3与check-axi全通过；新增boot09 exit0，控制台标记、启动链及AXI检查通过。
- M2-3完整回归本轮仅审阅归档成功记录，未独立重跑24次构建；CPU/SoC本轮未重新构建，单元已重新构建。未综合、未上板。

证据：codex-review/，独立入口codex-review/replay.py；4M3在codex-review/boot-gate/。复核脚本首次对SoC日志直接搜索整串标记失败，原因是HTIF字符与跟踪交错；改用既有check-m3控制台重建后通过，不是CPU故障。

## 两项限定收尾

1. 总入口run-cpu-m.sh仍生成缺tcpu_muldiv.v且未更新程序misa期望的旧回归脚本。修好的run-m23-with-m.sh没有接回总入口。已交付的core-run1/run-m2-3-with-m.sh及源码匹配可直接确认。必须接回正确入口并实际跑到最终成功；同处将负例从任意非零明确为特定结果检查失败。
2. 原任务要求CPU级长运算中复位、无陈旧写回；交付只有乘除单元级复位测试，核级证据缺失。补乘/除各一个命中S_MUL的定向例即可，不扩通用复位矩阵。

源码中核与运算单元同rst且复位清状态/写使能，方向合理；但源码合理与动态验证完成分开记录。算术主体和正常接入限定认可，整项待以上收尾。任务codex-cpu-m-closeout已分派，通过后进入整数C。
