# M4-c 板测验收 — 2026-09-16

结论：限定通过首次裸机板测。认可session-5在attempt-2位流、每项启动前保持SYSTEM_RESET至少50ms的操作条件下四项程序通过；不认可“复位竞争已彻底解决”或任意运行中重配置安全。

本轮独立只读审查：m4-board/m4-build/m4-prep三份HASHES清单核验无错误；读取session-5原始console，分别看到四项实际输出TEACHING-CPU-M3-OK、M3-DDR-OK、M3-CLINT-OK、M3-BADADDR-OK及对应返回0，不只依赖test摘要。每项之前有50ms复位保持。回退日志有baseline加载返回0、SYSTEM_RESET=1、Rocket hello实际PASS与返回0。coord无持有租约。没有重新连接板卡或重跑硬件测试。

重要变化：交付已不再是原attempt-1，而是修改ReqRespToTL的attempt-2。已阅读新桥接收并丢弃空闲态D的实现及回归报告；既有回归按报告未产生孤儿，因此不能据其全过称新增复位行为已被定向覆盖。本轮未独立重建或重放全部仿真，回归运行结论仍归Claude交付证据。

根因证据支持复位与在途DDR响应相关的故障假设及规程改善，但无板上周期级总线轨迹，具体“孤儿D堵塞所有客户端”链条应标作代码分析解释，不是逐周期实测。无保持条件下仍有挂起，不能自动把ready高当作跨复位事务语义完整正确。旧响应若在新请求sA/sD阶段返回，仍可能被当作新响应；固定50ms是本板测试操作条件，不是协议级排空完成证明。

认可当前四项裸机功能结果及已恢复Rocket的最后记录；尚未完成M/C、MMU、xv6，也未证明最后退出store退休尾部。mtime400kHz/DTB1MHz差异未处理。首次运行中重配置引起PS卡死及此前失败记录应继续保留。

下一步优先提交复位/事务排空设计与定向验证方案：明确哪些模块保持运行以排空、如何阻止新请求、复位期间及释放边界的旧D处理、排空确认或有依据的上界，以及未完成事务时禁止PL重配置。应有旧D在idle/新A/新D各阶段到达的仿真反例，检查不误交付、不重复写和不会死锁。先审方案，不自动批准以固定周期延展替代完整协议或启动新硬件实验。
